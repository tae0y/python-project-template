# commands.nouse — Disabled Commands

Commands here are disabled. To enable a command, move its file into `.claude/commands/`.

## Why each command is disabled

| Command | Disabled reason |
|---------|----------------|
| `review-team.md` | Agent Teams 전용 리뷰 커맨드. 고비용(팀원 5명 × 최대 15턴). `.claude/` 구조를 정기적으로 감사할 때만 수동으로 활성화할 것. 일상 개발 세션에서는 불필요. |
